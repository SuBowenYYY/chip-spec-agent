# PDF 转 Markdown 用户提示词

请将以下芯片规格书的 {total_pages} 图片按照系统提示词中定义的输出模板，完整解析并生成结构化的 Markdown 摘要文档。

## 要求

1. 严格按照输出模板的章节结构和格式要求输出
2. 所有参数值必须从图片中精确提取，不得臆测或推断
3. 表格中的每一行参数都必须完整提取，禁止遗漏
4. 如果某章节在规格书中不存在，按缺项规范处理
5. 只输出 Markdown 文档本身，不附加任何解释或说明
